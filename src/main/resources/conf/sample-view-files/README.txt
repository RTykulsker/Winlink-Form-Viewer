This directory contains sample Winlink "view" files.

You can see how the WinlinkFormViewer (fv) works, either by:
1) uploading these files using the file-upload button
2) dragging and dropping one of these files on the web page
